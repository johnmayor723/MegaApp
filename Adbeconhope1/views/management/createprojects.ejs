<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Project</title>

  <!-- ✅ Bootstrap 5 CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />

  <script>
    function addImageInput() {
      const container = document.getElementById('imageInputs');
      const input = document.createElement('input');
      input.type = 'file';
      input.name = 'images';
      input.className = 'form-control mb-2';
      container.appendChild(input);
    }
  </script>

  <style>
    body {
      background-color: #f8f9fa;
    }
    .navbar-brand {
      font-weight: 700;
      color: #141414 !important;
      font-size: 1.3rem;
      letter-spacing: 0.5px;
    }
    .card {
      border: 1px solid #dee2e6;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      background-color: #fff;
    }
    .card h1 {
      color: #1b1b1c;
      font-size: 1.8rem;
      font-weight: 600;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
   <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm">
    <div class="container">
      <a class="navbar-brand" href="/">AdedoyinBeaconOfHopeFoundation</a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/management/projects">Projects</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/management/projects/new">Create Project</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-danger" href="/">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Form Container -->
  <div class="container mt-5">
    <div class="card p-5">
      <h1 class="mb-4 text-center">Create Project</h1>

      <form action="/management/projects" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label class="form-label fw-semibold">Title</label>
          <input type="text" name="title" class="form-control" placeholder="Enter project title" required>
        </div>

        <div class="mb-3">
          <label class="form-label fw-semibold">Description</label>
          <textarea name="description" class="form-control" rows="4" placeholder="Enter project description" required></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label fw-semibold">Goal (Target Amount)</label>
          <input type="number" name="goal" class="form-control" min="0" step="0.01" placeholder="Enter target goal amount" required>
        </div>

        <div class="mb-3">
          <label class="form-label fw-semibold">Raised Amount</label>
          <input type="number" name="raised" class="form-control" min="0" step="0.01" placeholder="Amount raised so far (optional)">
        </div>

        <div class="mb-3">
          <label class="form-label fw-semibold">Status</label>
          <select name="status" class="form-select" required>
            <option value="" disabled selected>Select project status</option>
            <option value="ongoing">Ongoing</option>
            <option value="completed">Completed</option>
            <option value="latest">Latest</option>
          </select>
        </div>

        <div id="imageInputs" class="mb-3">
          <label class="form-label fw-semibold">Upload Images</label>
          <input type="file" name="images" class="form-control mb-2" accept="image/*">
        </div>

        <button type="button" onclick="addImageInput()" class="btn btn-outline-secondary mb-3">
          + Add More Images
        </button>

        <div class="text-center">
          <button type="submit" class="btn btn-success px-4">Save Project</button>
        </div>
      </form>
    </div>
  </div>

  <!-- ✅ Bootstrap 5 JS -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
</body>
</html>
