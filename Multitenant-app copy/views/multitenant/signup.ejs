<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>EasyApps || Sign-up</title>
  <link rel="icon" type="image/png" href="/multitenant/img/easyicon.png">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root {
      --left-bg: #f8f9fa; /* light gray for form column */
      --right-bg-img: url('/multitenant/img/signup-bg.jpg'); /* change to your background image */
    }

    html, body {
      height: 100%;
      margin: 0;
    }

    .row-full {
      min-height: 100vh;
    }

    .g-0 {
      --bs-gutter-x: 0;
      --bs-gutter-y: 0;
    }

    /* Left column (form side) */
    .col-left {
      background: var(--left-bg);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .signup-box {
      width: 100%;
      max-width: 420px;
      background: #ffffff;
      border-radius: 12px;
      padding: 28px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.06);
      text-align: center;
    }

    .signup-logo {
      font-size: 1.6rem;
      font-weight: 700;
      margin-bottom: 1.25rem;
    }

    .signup-heading {
      margin-bottom: 100px;
    }

    .btn-signup {
      margin-bottom: 12px;
      width: 100%;
      height: 44px;
      border-radius: 8px;
      font-weight: 600;
    }

    .btn-apple {
      background: #000;
      color: #fff;
      border: none;
    }

    .btn-google {
      background: #fff;
      border: 1px solid #cfcfcf;
      color: #222;
    }

    .form-email {
      margin-bottom: 18px;
    }

    /* Right column (full background image) */
    .col-right {
      background-image: var(--right-bg-img);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      position: relative;
    }

    /* Optional overlay for readability */
    .col-right::before {
      content: "";
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.3); /* darken the image a bit */
    }

    /* optional centered logo or text on image */
    .right-content {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100%;
      color: #fff;
      text-align: center;
      padding: 2rem;
      background: linear-gradient(
    180deg,
    #fbfafa 0%,
    #81dcf5 25%,
    #b5b4b4 55%,
    hwb(311 69% 1%) 100%
  );
  color: #111;
  min-height: 100vh;
  background-attachment: fixed;
}
    }

    .right-content img {
      max-width: 200px;
      margin-bottom: 1rem;
      filter: brightness(0) invert(1);
    }

    .right-content h2 {
      font-weight: 600;
      font-size: 1.5rem;
      line-height: 1.4;
    }

    /* Mobile adjustments */
    @media (max-width: 767.98px) {
      .col-right {
        display: none;
      }

      .col-left {
        background-color: #fff;
        padding: 1rem;
      }

      .signup-box {
        box-shadow: none;
        padding: 1rem;
      }

      .signup-heading {
        margin-bottom: 24px;
      }

      .signup-logo {
        font-size: 1.25rem;
      }
    }

    /* Input & Button */
    .input-group .form-control {
      height: 44px;
      border-radius: 8px 0 0 8px;
    }

    .input-group .btn {
      height: 44px;
      border-radius: 0 8px 8px 0;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="row g-0 row-full">
    <!-- Right: Background Image Section -->
    <div class="col-12 col-md-6 col-right">
      <div class="right-content">
        <img src="/multitenant/img/easylogo.png" alt="EasyApps Logo" onerror="this.style.display='none'">
        <h2>Welcome to EasyApps</h2>
        <p class="lead">Create and manage your applications seamlessly.</p>
      </div>
    </div>

    <!-- Left: Form Section -->
    <div class="col-12 col-md-6 col-left">
      <div class="signup-box" aria-labelledby="signup-heading">
        <div class="mobile-header d-md-none mb-3">
          <% if (typeof success_msg !== "undefined" && success_msg && success_msg.length > 0) { %>
            <div class="alert alert-success"><%= success_msg %></div>
          <% } %>
          <% if (typeof error_msg !== "undefined" && error_msg && error_msg.length > 0) { %>
            <div class="alert alert-danger"><%= error_msg %></div>
          <% } %>
          <img class="signup-image" height="40" src="/multitenant/img/easylogo.png" alt="EasyApps" onerror="this.style.display='none'">
        </div>

        <div id="signup-heading" class="signup-logo signup-heading" style="margin-top: 80px;">
          🚀 Create An Account For Free
        </div>

        <form class="form-email" action="/multitenant/request-otp" method="POST" novalidate>
          <div class="input-group mb-2">
            <input
              type="email"
              name="email"
              class="form-control"
              placeholder="Enter your email"
              aria-label="Email address"
              required
            />
            <button class="btn btn-dark" type="submit">Continue with Email</button>
          </div>
          <div class="text-muted small mb-3">
            We’ll send a confirmation link to this email.
          </div>
        </form>

        <button class="btn btn-google btn-signup" type="button">🔵 Continue with Google</button>
        <button class="btn btn-apple btn-signup" type="button"> Continue with iPhone</button>

        <div style="margin-top:12px; font-size:0.95rem;">
          Already have an account? <a href="/multitenant/login" class="fw-semibold">Sign in</a>
        </div>
      </div>
    </div>
  </div>
  <a
  href="https://wa.me/2349123907060"
  target="_blank"
  class="whatsapp-float"
  aria-label="Chat with us on WhatsApp"
>
  <i class="fab fa-whatsapp"></i>
</a>
<style>
 .whatsapp-float {
  position: fixed;
  bottom: 24px;
  right: 24px;
  width: 56px;
  height: 56px;
  background-color: #25d366;
  color: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30px;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
  z-index: 9999;
  text-decoration: none;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.whatsapp-float:hover {
  transform: scale(1.08);
  box-shadow: 0 12px 28px rgba(0, 0, 0, 0.4);
}

  </style>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
